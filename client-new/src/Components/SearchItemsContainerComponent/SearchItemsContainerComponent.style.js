import styled from 'styled-components';

export const div = styled.div`
   width: 100%;
   padding: 0.7rem;
   position: absolute;
   top: 90%;
   left: 0;
   z-index: 100;
   background-color: var(--sm-border-cl);
   border-radius: 5px;
   height: 350px;
   overflow-x: hidden;
`;
