import styled from 'styled-components';

export const div = styled.div`
   width: 100%;
   border-radius: 2px;
   background-color: var(--smooth-lg-sl-cl);
   padding: 1rem;
   text-align: center;

   p {
      font-size: 12px;
   }
`;
