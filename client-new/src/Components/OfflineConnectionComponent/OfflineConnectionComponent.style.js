import styled from 'styled-components';

export const div = styled.div`
  position: absolute;
  bottom: 0;
  left: 0;
  width: 100%;
  background-color: var(--dark-red-cl);
  text-align: center;
  color: var(--main-cl);
  font-size: 12px;
`;
